#################
物理系基科32 付亮
Note:
这几天各种期末考试，就只实现了主要功能。>.<
#################

运行要求:
1.需要data.txt在同一目录下，直接在终端运行 query.py 即可。


使用介绍:
1.每次输入关键词,然后敲击回车键即可显示结果, prefix是指以这个词为前缀的个数(不包含该词条本身)，word是指(该词条出现了多少次)。
2.不设置结束，要退出程序在终端退出即可。(偷懒)
3.搜索结果最多显示15条

算法：
依照老师给出的tries树算法，设置类edge，存储前缀、单词树、子词条edge




